import SettingsPageWrapper from "./settings-wrapper";

// metadata
export const metadata = {
  title: "Settings",
  description: "Manage your dashboard settings and preferences.",
};

export default function SettingsPage() {
  return <SettingsPageWrapper />;
}
